//factorial of a number
#include <iostream>

using namespace std;

int main()
{
    int n;
    int f = 1, i = 1;
    cout<<"Enter the no. : ";
    cin>>n;
    while(i<=n){
        f *= i;
        i++;
    }
    cout<<endl<<"The factorial of "<<n<<" is "<<f;

    return 0;
}
